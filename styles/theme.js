import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {  
      main: "#f03838",
    },
    // text: {
    //   primary: "#fff",
    //   secondary: "#fff",
    //   disabled: "#fff",
    // },
  },
});

export default theme;
